{
  "name": "SSE",
  "version": "1.0.0",
  "description": "An implementation of SSE (Server Sent Events) server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp scripts",
    "start": "tsc -p ./ && concurrently \"tsc -w -p ./\" \"nodemon dist/index.js\""
  },
  "author": "sahil.khanna",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-validator": "^5.0.3"
  },
  "devDependencies": {
    "@types/debug": "^4.1.5",
    "@types/express": "^4.17.2",
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2",
    "tslint": "^5.20.1",
    "typescript": "^3.7.4"
  }
}
